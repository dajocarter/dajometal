<ul class="nav social">
  {{#each site.social}}
    <li>
      <a target="_blank" class="{{ title }}" href="{{ link }}" title="{{ title }} {{ handle }}">
        <i class="icon {{ icon }}"></i>
      </a>
    </li>
  {{/each}}
</ul>