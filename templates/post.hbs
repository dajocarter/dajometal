{{> header }}
  <main>
    <article class="wrap">
      <h1>{{ title }}</h1>

      <div class="post-content">
        {{{ contents }}}
      </div>

      <footer>
        {{> author }}
      </footer>
    </article>
  </main>
{{> footer }}